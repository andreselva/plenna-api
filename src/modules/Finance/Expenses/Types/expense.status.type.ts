export enum ExpenseStatus {
    PENDING = 'pending',
    PAID = 'paid',
    PARTIAL = 'partial',
    CANCELED = 'cancelled',
    REVERSED = 'reversed',
    DELETED = 'deleted',
    ARCHIVED = 'archived'
}