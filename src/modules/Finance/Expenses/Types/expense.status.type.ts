export enum ExpenseStatus {
    PENDING = 'pending',
    PAID = 'paid',
    PARTIAL = 'partial',
    CANCELLED = 'cancelled',
    REVERSED = 'reversed',
    DELETED = 'deleted',
    ARCHIVED = 'archived'
}